{
  "name": "mugen-monorepo",
  "private": true,
  "scripts": {
    "build:sdk": "pnpm --filter @mugen/sdk build",
    "build:ui": "pnpm --filter mugen-ui build",
    "build": "pnpm build:sdk && pnpm build:ui"
  }
}